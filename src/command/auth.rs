
pub fn auth() {
    // Open default browser (using https://docs.rs/webbrowser/latest/webbrowser/)
    // to 'https://dashboard.render.com/u/settings#api-keys'
    // and wait for user to copy the token into the prompt.
    // Then, save it to ~/.config/render/auth.json.

}